After finding the amicable pairs, add up all the amicable numbers and display the sum.

Code for calculating sum-
int divisorsum(int n)
{
  int sum=0, i;
  for(i=1;i<n;i++)
  {
    if(n%i==0)
    {
      sum=sum+i;
    }
  }
 return sum;
 }
