After the calculation of sum of all the divisors, we will specify the condition for checking of amicable pair.
C program code for finding amicable numbers

int a,b;
for(a=1;a<=10000;a++)
  {
    for(b=1;b<=10000;b++)
    {
      int d(int)
      {
        if((d(a)==(b)) && (d(b)==(a)) && (a!=b);
        {
          printf("\n %d%d",a,b);
        }
      }
     }
     return 0;
    }
